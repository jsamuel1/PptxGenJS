// Type definitions for @jsamuel1/pptxgenjs/utils/color
// Tree-shakeable subset of the /utils barrel — colour conversion + WCAG contrast helpers.
// Re-exported from ./utils (single source of truth) to guarantee parity with the barrel.
export {
	cssNamedColorToHex,
	normalizeColor,
	relativeLuminance,
	contrastRatio,
	inkForFill,
} from './utils'
